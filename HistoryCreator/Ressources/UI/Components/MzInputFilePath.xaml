<UserControl x:Class="HistoryCreator.Ressources.UI.Components.MzInputFilePath"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HistoryCreator.Ressources.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Label x:Name="LabelPart"
               Grid.Row="0"
               Grid.ColumnSpan="2" />
        <TextBox x:Name="DataPart"
                 Grid.Row="1"
                 Grid.Column="0" />
        <Button x:Name="DialogButton"
                Grid.Row="1"
                Grid.Column="1"
                Content="..."
                Margin="5 0 0 0"
                Width="24" />
    </Grid>
</UserControl>
